# XpertAgentNotes

```bash
# 初始化仓库
git init .
git remote add origin git@github.com:rookie-littleblack/XpertAgentNotes.git
git add .
git commit -m "initial commit"
git branch  # 查看分支
git branch -m master main  # 重命名分支
git branch  # 查看分支
git config user.name "rookielittleblack"
git config user.email "rookielittleblack@yeah.net"
git push -u origin main  # 推送到远程分支
```